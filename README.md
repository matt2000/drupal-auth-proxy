# Dependencies
- NodeJS v.0.10+
- npm package manager

# Install
- `npm install`

# Configure
- Copy `config/default.json` to `config/your.hostname.json` and fill in desired
  values.

# Run
- See or Execute `./start.sh`
